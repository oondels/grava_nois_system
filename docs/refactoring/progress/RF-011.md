# RF-011 — Novo runtime e cutover

- Status: REVIEW
- Owner: architecture_lead
- Reviewer: quality_reliability
- Branch: refactor/clean-architecture
- Commits: `326a9bd`, `994b578`
- Dependências: RF-007, RF-009, RF-010

## Objetivo
Compor adapters/casos de uso e trocar o runtime após equivalência.
## Escopo
`EdgeRuntime`, container explícito, entrypoint mínimo, testes comparativos e cutover. Fora de escopo: remover legado.
## Critérios de aceitação
Wiring não contém regras; runtime novo equivale ao antigo nos contratos; shutdown e degradação funcionam; rollback é possível.
## Snapshot atual
O container explícito e o `EdgeRuntime` foram implementados em `326a9bd`; os
adapters concretos de processo de captura e repositório de segmentos foram
adicionados em `994b578`. Ciclo de vida e adapters estão testados, mas a
implementação continua em revisão. O cutover não ocorreu: `main.py`, workers e
integrações legadas continuam sendo o runtime ativo.
## Decisões e ADRs
ADR-001 e ADR-002.
## Arquivos modificados
`src/bootstrap/`, adapters de captura em `src/infrastructure/media/`,
`tests/test_bootstrap_runtime.py` e `tests/test_runtime_capture_adapters.py`.
## Testes, comandos e resultados
- Baseline de entrada: `pytest` com 259 passed e 2 skipped.
- Coverage de entrada de `domain`/`application`: 98%.
- Ruff no escopo e mypy: verdes.
- `.venv/bin/python -m pytest -q tests/test_bootstrap_runtime.py`: 4 passed.
- Adapters de captura e buffer cobertos por
  `tests/test_runtime_capture_adapters.py`.
- Testes comparativos do entrypoint real e smoke test com integrações reais:
  pendentes.
## Riscos e bloqueios
O cutover está bloqueado até que:

- o reviewer aprove container, ownership e ordem de shutdown;
- o wiring conecte os adapters concretos sem reintroduzir dependências globais;
- testes comparativos provem equivalência com o entrypoint legado;
- smoke tests confirmem degradação e shutdown com integrações reais;
- um caminho de rollback seja documentado e exercitado.

Até esses gates, manter fachadas e runtime legado ativos.
## Próximo passo exato
O `quality_reliability` deve revisar `326a9bd` e `994b578`, executar os testes
comparativos e registrar as evidências. Não trocar o entrypoint durante a
revisão.
## Log de sessões
- 2026-07-27: todas as dependências concluídas e gates integrados verdes; task
  promovida a `READY`, sem cutover.
- 2026-07-27: container e `EdgeRuntime` implementados em `326a9bd`; 4 testes de
  bootstrap aprovados; task movida para `REVIEW`, sem cutover.
- 2026-07-27: adapters concretos de captura adicionados em `994b578`; blockers
  de wiring, equivalência, smoke e rollback permanecem; sem cutover.
## Resumo final
Implementação do composition lifecycle concluída; cutover e aprovação final
pendentes.
## Próxima task desbloqueada
RF-012 está em `REVIEW`, sem pressupor cutover concluído.
