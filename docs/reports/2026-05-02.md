# Relatorio Diario - 2026-05-02

## Pre-dev: recuperacao RTSP e healthcheck Docker

**Objetivo**: corrigir o caso em que a camera volta a ficar acessivel, mas o edge permanece sem segmentos novos porque o FFmpeg continua vivo e o supervisor nao reinicia o pipeline. Corrigir tambem o `unhealthy` falso causado por healthcheck Docker dependente de `pgrep` ausente na imagem slim.

**Contexto lido**:

- `docs/specs/system/PIPELINE.md`
- `docs/specs/system/BUSINESS_RULES.md`
- `docs/specs/system/ARCHITECTURE.md`
- `main.py`
- `src/video/capture.py`
- `src/video/buffer.py`
- `tests/test_trigger_fanout.py`
- `docker-compose.yml`, `docker-compose-dev.yml`
- `grava_nois_config.git/main/docker-compose.yml`
- `grava_nois_config.git/main/.setup_ubuntu_server.sh`

**Diagnostico**:

- `_camera_supervisor()` reinicia somente quando `proc is None` ou `proc.poll() is not None`.
- Quando o FFmpeg permanece vivo mas para de gravar segmentos, `_camera_readiness()` marca a camera como `UNAVAILABLE`, mas o supervisor nao executa acao corretiva.
- O healthcheck `pgrep -f 'python.*main'` falha em imagens base `python:3.11-slim` sem `procps`.

**Impacto previsto**:

- Alterar somente o processo FFmpeg da camera afetada; nao reiniciar container por buffer stale.
- Preservar `capture.trigger_rejected` para triggers durante indisponibilidade.
- Manter Docker healthcheck medindo liveness do processo Python, nao saude da camera.
- Atualizar docs/specs porque o comportamento documentado de buffer stale sera alterado.

**Plano de teste**:

- Adicionar teste unitario para restart por buffer `STALE` persistente.
- Adicionar teste unitario para garantir que `STALE` transitorio nao reinicia.
- Rodar testes focados de trigger/presenca/supervisor.
- Validar sintaxe Python e compose dos dois repositorios.

READY TO IMPLEMENT: YES

---

## Implementacao: recuperacao RTSP e healthcheck

**Acao**: implementado restart automatico do FFmpeg por camera quando o `SegmentBuffer` fica `STALE`/`MISSING`/`UNKNOWN` de forma persistente, sem reiniciar o container. O healthcheck Docker foi alterado para usar Python e `/proc/1/cmdline`, removendo dependencia de `pgrep`.

**Arquivos alterados**:

- `main.py` - adiciona politica de stale persistente e encerramento controlado do FFmpeg.
- `tests/test_trigger_fanout.py` - cobre stale persistente com restart e stale transitorio sem restart.
- `docker-compose.yml` e `docker-compose-dev.yml` - healthcheck sem `pgrep`.
- `README.md`, `CHANGELOG.md`, `docs/specs/system/*` - contrato operacional atualizado.

**Validacao**:

- `PYTHON_DOTENV_DISABLED=1 PYTHONPATH=. python -m unittest tests.test_trigger_fanout tests.test_device_presence_service` - OK.
- `python -m py_compile main.py src/video/capture.py src/video/buffer.py tests/test_trigger_fanout.py` - OK.
- `COMPOSE_DISABLE_ENV_FILE=1 docker compose config --no-env-resolution --no-path-resolution` - OK.
- `COMPOSE_DISABLE_ENV_FILE=1 docker compose -f docker-compose-dev.yml config --no-env-resolution --no-path-resolution` - OK.

**Observacao**: a primeira execucao dos testes sem `PYTHON_DOTENV_DISABLED=1` falhou antes dos testes por permissao negada no `.env` local do checkout.

Status: COMPLETO
