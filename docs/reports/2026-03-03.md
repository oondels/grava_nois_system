# Relatorio Diario — 2026-03-03

## [Pre-dev] Estabilizacao de captura RTSP para eliminar microcortes
**Task**: Corrigir travamentos no highlight final ("pequenos cortes continuos") com camera Tapo C500.

### PRE-DEV REPORT: fix(edge): estabilizar timestamps na captura RTSP segmentada

**GOAL**  
Eliminar microtravamentos no video final ajustando o pipeline de captura/segmentacao RTSP para produzir segmentos temporalmente continuos.

**DEFINITION OF DONE**  
1. Captura RTSP nao deve gerar regressao estrutural no fluxo (`main -> start_ffmpeg -> SegmentBuffer -> build_highlight`).  
2. Configuracao padrao deve privilegiar fluidez (timestamps estaveis e cortes em keyframe).  
3. Deve existir opcao de compatibilidade para manter passthrough (`copy`) quando necessario.  
4. Testes automatizados devem validar montagem do comando FFmpeg RTSP (modo padrao e modo legado).  
5. Nenhuma Hard Rule do dominio deve ser violada.

**CODEBASE READING COMPLETE**  
- `specs/grava-nois/DESIGN_SPEC.md`  
- `grava_nois_system/docs/design/DESIGN_SPEC.md`  
- `grava_nois_system/src/video/capture.py`  
- `grava_nois_system/src/video/processor.py`  
- `grava_nois_system/src/video/buffer.py`  
- `grava_nois_system/main.py`  
- `grava_nois_system/src/config/settings.py`  
- `grava_nois_system/tests/test_connection_logs.py`  
- `grava_nois_system/tests/test_no_file_collisions.py`  
- `grava_nois_system/tests/test_multi_camera_settings.py`  
- `grava_nois_system/tests/test_legacy_compatibility.py`  
- `grava_nois_system/logs/ffmpeg_cam01.log`  
- `grava_nois_system/logs/app.log`

**REUSE ASSESSMENT**  
- Reuso parcial: pipeline atual de buffer/concat/remux sera mantido.  
- Adaptacao necessaria: comando RTSP em `start_ffmpeg` sera ajustado para modo estavel por padrao, com fallback explicito para passthrough legado.  
- Justificativa: menor risco de cascata (mantem arquitetura), atuando no ponto de causa raiz (DTS nao-monotonico na origem).

**HARD RULES CHECK (AGENTS.md §1)**  
- HR-001, HR-002, HR-007, HR-008, HR-010: nao impactadas (sem alteracao de auth, RBAC, reset, webhook, HMAC device).  
- HR-003, HR-004, HR-005, HR-006, HR-009: sem alteracao de regras de upload/finalizacao/janela; mudanca restrita ao subsistema local de captura FFmpeg.

**IMPACT MAP**  
- Arquivo direto: `src/video/capture.py` (comando FFmpeg RTSP).  
- Arquivos de validacao: novo teste dedicado em `tests/` para montar/validar linha de comando.  
- Callers afetados: `main.py` chama `start_ffmpeg` sem mudanca de assinatura.  
- Cascata esperada: apenas caracteristica de captura RTSP (fluidez x uso de CPU).  
- Limite de escopo seguro: nao alterar `processor.py`, regras de negocio de upload/API, nem workflow de workers.

**TEST PLAN**  
- Unit:
  - validar comando RTSP padrao com recodificacao e keyframe alinhado ao `seg_time`;
  - validar modo legado passthrough com `copy`.
- Regressao:
  - executar testes existentes de conectividade/logs para garantir compatibilidade.
- Comandos:
  - `cd grava_nois_system && PYTHONPATH=. pytest -q tests/test_capture_ffmpeg_command.py`
  - `cd grava_nois_system && PYTHONPATH=. pytest -q tests/test_connection_logs.py`

**READY TO IMPLEMENT**: YES

---

## [Execucao] Patch aplicado
**Arquivos alterados**:
- `src/video/capture.py`
  - RTSP passou a usar modo estavel por padrao (reencode `libx264`) com:
    - `-fflags +genpts+discardcorrupt`
    - `-use_wallclock_as_timestamps 1`
    - keyframe forcado por segmento (`-force_key_frames expr:gte(t,n_forced*seg_time)`)
    - `-reset_timestamps 1`
  - adicionado fallback legado opcional via `GN_RTSP_PASSTHROUGH=1` (modo `copy`).
  - novas envs de tuning: `GN_RTSP_GOP`, `GN_RTSP_PRESET`, `GN_RTSP_CRF`.
- `tests/test_capture_ffmpeg_command.py` (novo)
  - valida comando padrao estavel;
  - valida modo legado passthrough;
  - valida aplicacao dos parametros de tuning por env.
- `README.md`
  - documentado novo comportamento RTSP e variaveis de estabilidade.

## [Validacao] Testes
**Comandos**:
- `PYTHONPATH=. pytest -q tests/test_capture_ffmpeg_command.py tests/test_connection_logs.py`
- `PYTHONPATH=. pytest -q`

**Resultado**:
- Focados: `8 passed`
- Suite completa: `61 passed`

---

## [Pos-rebuild] Validacao operacional da captura RTSP (22:03-22:07)
**Objetivo**: confirmar eliminacao de travamentos apos rebuild forcado do container e validar se ainda ha ajuste obrigatorio.

**Evidencias coletadas**:
- `logs/ffmpeg_cam01.log`
  - comando ativo em runtime: `-c:v libx264 ... -use_wallclock_as_timestamps 1 -fflags +genpts+discardcorrupt -g 30 -force_key_frames expr:gte(t,n_forced*1) -reset_timestamps 1`
  - nao houve novas ocorrencias de `Non-monotonic DTS`.
  - ocorreu aviso: `More than 1000 frames duplicated`.
- `logs/app.log` (linhas finais)
  - highlights de `22:02`, `22:04` e `22:06` foram gerados com `Total de segmentos selecionados: 30` (pre/post completos).
  - highlight alvo registrado: `highlight_cam01_20260303-220616-102033Z.mp4` salvo e processado com sucesso no worker.

**Resultado**:
- Diagnostico principal: correcao aplicada entrou em vigor e removeu o sintoma critico anterior (DTS nao-monotono), que era a principal causa dos microcortes.
- Risco residual: warning de duplicacao de frames sugere jitter/perda na origem RTSP ou mismatch temporario de clock da camera/rede, mas sem evidencia de regressao estrutural no pipeline segmentado.

**Pendencias**:
- O arquivo informado para analise (`failed_clips/upload_failed/highlight_cam01_20260303-220616-102033Z.mp4`) nao estava presente no filesystem local no momento da auditoria.
- Portanto, esta validacao foi concluida por logs operacionais; inspeção de frame pacing do MP4 final ficou pendente por indisponibilidade do arquivo.

**Ajuste obrigatorio restante**:
- Nenhum ajuste de codigo obrigatorio identificado nesta rodada.
- Recomendacao operacional: manter monitoramento do warning de `frames duplicated`; se crescer ou voltar travamento visual, aplicar tuning de rede/camera (bitrate CBR moderado, I-frame em 1s na camera quando disponivel, RTSP sobre TCP dedicado).

---

## [Pre-dev] Completar variaveis de ambiente em .env.example
**Task**: auditar variaveis lidas em runtime e garantir que `.env.example` e `README.md` cubram todos os knobs operacionais.

### PRE-DEV REPORT: docs(edge): alinhar .env.example com variaveis reais do sistema

**GOAL**
Garantir que nenhuma variavel de ambiente usada em `grava_nois_system` fique oculta para operacao/deploy.

**DEFINITION OF DONE**
1. Levantar todas as variaveis lidas no codigo (`os.getenv` + literais `GN_*` usados por helpers).
2. Comparar com `.env.example` e adicionar as faltantes com default seguro e comentario de uso.
3. Atualizar `README.md` para documentar variaveis avancadas/aliases que podem gerar duvida.
4. Validar que nao houve mudanca funcional de runtime (apenas docs/config sample).
5. Commit unico, focado, com Conventional Commit.

**CODEBASE READING COMPLETE**
- `grava_nois_system/.env.example`
- `grava_nois_system/README.md`
- `grava_nois_system/main.py`
- `grava_nois_system/src/config/settings.py`
- `grava_nois_system/src/video/capture.py`
- `grava_nois_system/src/services/api_client.py`
- `grava_nois_system/src/workers/processing_worker.py`
- `grava_nois_system/src/utils/{logger.py,pico.py,time_utils.py,device.py}`

**REUSE ASSESSMENT**
- Reuso total da estrutura existente de documentacao; sera feita apenas consolidacao de variaveis e comentarios.
- Sem criacao de nova infraestrutura de config.

**HARD RULES CHECK (AGENTS.md §1)**
- HR-001..HR-010: nao impactadas (sem alteracao de auth/upload/webhook/hmac em runtime; apenas documentacao/sample de env).

**IMPACT MAP**
- Arquivo principal: `.env.example`
- Arquivo complementar: `README.md` (secao de configuracao)
- Sem impacto em `src/` executavel.

**TEST PLAN**
- Verificacao esttica: conferir que todas variaveis usadas no codigo estao presentes na documentacao.
- Verificacao de regressao: sem execucao de testes automatizados obrigatoria por nao haver mudanca de comportamento.

**READY TO IMPLEMENT**: YES
