# Relatorio Diario - 2026-04-06

## Pre-dev

Tarefa: hardening pontual do `grava_nois_system` para logs/diagnostico de retry, topicos MQTT e contagem de fila.

Plano:

- Sanitizar respostas de backend persistidas por `retry_upload.py` para nao gravar `upload_url`/URLs assinadas em sidecars locais.
- Validar `device_id` usado em `MQTTConfig.topic_for()` para rejeitar `/`, `+`, `#` e byte nulo antes de publicar topicos, sem derrubar captura/worker quando MQTT estiver mal configurado.
- Trocar contagem de fila MQTT de `len(list(glob(...)))` para iterador.
- Atualizar README, CHANGELOG e specs afetadas.
- Validar com testes focados e suite completa.

## DoD

- Nenhum `.env` lido.
- Nenhuma execucao direta contra banco.
- Nenhum commit antes de aprovacao.
- Testes de regressao do edge executados apos a alteracao.
