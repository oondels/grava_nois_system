API_BASE_URL=""
CLIENT_ID=""
VENUE_ID=""

# Device
DEVICE_ID=""
DEVICE_SECRET=""
GN_HMAC_DRY_RUN="false"

GN_LIGHT_MODE=false
GN_INPUT_FRAMERATE=30
GN_SEG_TIME=1
GN_VIDEO_SIZE=1280x720
GPIO_PIN=17
GN_GPIO_COOLDOWN_SEC=120
GN_GPIO_DEBOUNCE_MS=300

GN_RTSP_PRE_SEGMENTS=3
GN_RTSP_POST_SEGMENTS=1

# Fonte do acionamento do botão: "gpio", "pico", "auto" ou "both"
GN_TRIGGER_SOURCE

# Token enviado pelo raspberry pico para autenticar o acionamento do botão
GN_PICO_TRIGGER_TOKEN="BTN_REPLAY"

# Configuração da câmera RTSP
# GN_RTSP_URL="rtsp://<user>:<senh>@<ip>:<port>/cam/realmonitor?channel=1&subtype=0"

# Health check RTSP: configurações de retry e timeout
GN_RTSP_MAX_RETRIES=10  # Número de tentativas de conexão (10 x 5s = até 50s de espera)
GN_RTSP_TIMEOUT=5       # Timeout por tentativa em segundos

# Diretório para logs do FFmpeg (facilita debug de problemas de conexão)
# GN_LOG_DIR=/usr/src/app/logs
# DEV="true"
DEV_VIDEO_MODE="true"